#' @details A `\emph{x}` macro and a plain `code` span.
#'
#' Nested `\code{\link{y}}` and two-arg `\href{https://e.org}{d}`.
#'
#' Escaped `\\emph{x}` stays one span; `\dots{}` stops at the name.
#'
#' A double span: ``\emph{y}`` and ``a `b` c``.
#' @name rd_macro_in_code_span
NULL

#' @md
#' @details Under markdown `\emph{x}` is a code span.
#' @name rd_macro_in_code_span_md
NULL
