{
  x <- if (condition) # before consequence
    consequence # after consequence
  else
    alternative

  y <- if (condition)
    # before consequence
    consequence
  # before alternative
  else
    alternative
}
