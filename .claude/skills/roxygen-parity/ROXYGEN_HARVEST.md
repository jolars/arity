# roxygen2 oracle (harvested corpus)

_Generated by `task roxygen-harvest` (`tests/roxygen_oracle.rs`). Do not edit by hand._

Standalone roxygen blocks mined from roxygen2's own test suite (`scripts/harvest-roxygen-corpus.R`). Each is checked for the fixed point `roxygen2(arity_format(x)) == roxygen2(x)`. Gated **opt-in** by `tests/oracle/roxygen-allowlist.txt`: allowlisted slugs are guarded against regression; **DIVERGENT** slugs are the backlog (grow the formatter/parser, then ratchet them into the allowlist). Not a build gate (needs R; `#[ignore]`d).

- **Corpus:** 217 cases (`tests/oracle/corpus/roxygen.jsonl`)
- **Preserving:** 215  (214 allowlisted)  ·  **Divergent (backlog):** 1
- **Skipped:** 0 (arity could not format) + 1 (roxygen2 could not render)

## Divergent (backlog)

These slugs change the rendered Rd when formatted --- the work to pick off.

| Slug |
|---|
| `rx-0a1710c0` |

