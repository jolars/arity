# Changelog

All notable changes to the Arity VS Code extension are documented here.

## 0.2.0

- Initial release: starts the `arity` language server (`arity lsp`) for R files,
  with bundled-binary, PATH, and GitHub-release download strategies.
