(\description (TEXT "Reference links that contain markup are dropped"))
(\details (TEXT "A reference is dropped because its display is not plain text, but") (\link (TEXT "plain")) (TEXT "and") (\code (\link (TEXT "code"))) (TEXT "survive while drops too."))
(\title (TEXT "Reference links that contain markup are dropped"))
