(\description (\if (TEXT "html") (\out (VERB "\n") (VERB "<div>trailing text rides in the block\n") (VERB "gathered too\n"))))
(\details (\if (TEXT "html") (\out (VERB "\n") (VERB "  <span>\n") (VERB "===\n") (VERB "indent kept and the underline is swallowed, not setext\n"))))
(\title (TEXT "HTML-block-from-value edges"))
