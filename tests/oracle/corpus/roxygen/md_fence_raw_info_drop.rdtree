(\description (\if (TEXT "html") (\out (VERB "<div class=\"sourceCode a{b}c\">"))) (\preformatted (VERB "y\n")) (\if (TEXT "html") (\out (VERB "</div>"))))
(\details)
(\title (TEXT "Raw fence info string drops or keeps the section"))
