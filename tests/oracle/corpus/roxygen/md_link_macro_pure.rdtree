(\description (TEXT "Pure-macro link displays"))
(\details (TEXT "An active pure-macro display") (\link (\emph (\emph (TEXT "x")))) (TEXT "drops, an inert one") (\link (\emph (TEXT "y"))) (TEXT "keeps, and a fragile one") (\link (\code (RCODE "f"))) (TEXT "keeps too."))
(\title (TEXT "Pure-macro link displays"))
