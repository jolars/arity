(\description (TEXT "Defs inside list items"))
(\details (TEXT "A definition that sits in a list item is consumed, and the reference") (\href (VERB "https://example.com") (TEXT "foo")) (TEXT "elsewhere in the field still resolves to it:") (\itemize (\item)))
(\title (TEXT "Defs inside list items"))
