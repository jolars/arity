(\description (TEXT "A fenced code block at the content column folds into a list item"))
(\details (\itemize (\item) (TEXT "a") (\if (TEXT "html") (\out (VERB "<div class=\"sourceCode\">"))) (\preformatted (VERB "code\n")) (\if (TEXT "html") (\out (VERB "</div>"))) (\item) (TEXT "b")))
(\title (TEXT "A fenced code block at the content column folds into a list item"))
