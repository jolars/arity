(\description (TEXT "Fenced code block from a tag's same-line value"))
(\details (\if (TEXT "html") (\out (VERB "<div class=\"sourceCode r\">"))) (\preformatted (VERB "f(1)\n")) (\if (TEXT "html") (\out (VERB "</div>"))) (TEXT "after the fence"))
(\title (TEXT "Fenced code block from a tag's same-line value"))
