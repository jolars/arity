(\description (TEXT "Multi-line inline HTML from a tag's same-line value"))
(\details (TEXT "a") (\if (TEXT "html") (\out (VERB "<!-- x\n") (VERB "y -->"))) (TEXT "b"))
(\title (TEXT "Multi-line inline HTML from a tag's same-line value"))
