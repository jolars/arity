#' Shortcut links that contain markup are dropped
#'
#' @details
#' A shortcut [*foo*] is dropped because its display is not plain text, but
#' [a_b] and [`code`] survive while [`x` `y`] drops too.
#' @md
#' @name md_shortcut_emphasis
NULL
