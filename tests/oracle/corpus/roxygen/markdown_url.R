#' Markdown URLs and autolinks
#'
#' See the homepage <https://example.org/home> for the project.
#' A bare reference [https://example.org/bare]() also becomes a URL, while a
#' named link [the docs](https://example.org/docs) stays an href.
#' @md
foo <- function() {}
