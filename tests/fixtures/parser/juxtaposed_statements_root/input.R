# Two expressions on one line need a newline or ';' between them.
# This shape shows up in scripts that paste `scan()` data below the call.
Y <- scan()
12 14 33

# A separator makes it legal again.
1; 2
x <- 1 # trailing comments are not statements
f(1) g(2)
